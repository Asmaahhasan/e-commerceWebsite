@model IEnumerable<FixIt.OrderItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OidNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SIdNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OidNavigation.Oid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SIdNavigation.SId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OitemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OitemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OitemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
